{
  "name": "velvet-words-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "cd 'New folder (5)/pulse-field' && npm install && npm run build",
    "dev": "cd 'New folder (5)/pulse-field' && npm run dev"
  }
}